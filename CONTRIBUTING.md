# Contributing

Run `pytest -q` and `python -m eval.run_eval` before opening a pull request. Keep policy logic deterministic, cite policy identifiers in every verdict, and add a golden case for any new decision rule. Never add a live credential or employee data to the repository.
